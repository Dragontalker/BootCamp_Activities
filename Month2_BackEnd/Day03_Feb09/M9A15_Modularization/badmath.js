const pie = ['apple', 'cheery', 'mashmellow'];

const predictable = () => Math.floor(Math.random() * 3);

// module.exports is an object we use to store variables or methods
module.exports = {
  pie,
  predictable,
};
