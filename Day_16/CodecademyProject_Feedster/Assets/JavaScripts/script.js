$(document).ready(() => {
    // Task 1: add an event handler that shows the .nav-menu element when a user mouse over the .menu element.
    $('.menu').on('mouseenter', () => {
        $('.nav-menu').show();
    })
}); 
